# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Локальные переменные окружения (ключи Firebase) — не коммитим
.env
.env.local
.env.*.local

# Ключ service account для scripts/secretary.mjs — НАСТОЯЩИЙ секрет,
# в отличие от публичного веб-конфига Firebase. В репозиторий не попадает.
service-account.json
*-service-account.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Медицинские данные — репозиторий публичный (Pages из main), в коммит не идут
medical-profile-ismail.md
ЗДОРОВЬЕ.md

# Личные документы: репозиторий публичный, эти файлы живут только локально.
# Там визы, долги, семья, здоровье — в коммит они больше не идут.
CLAUDE.md
ВОПРОСЫ.md

# Выгрузки сенсора Libre (глюкоза, ФИО в имени файла) — публичный репозиторий,
# в коммит не идут. Лежат в проекте только чтобы разбирать их рядом с дневником.
libre 3 sensor/
